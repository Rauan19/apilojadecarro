/**
 * @deprecated A vitrine `/loja/:slug` resolve a loja pelo slug/domínio.
 * Tokens de API ficam só para sites externos em `/api/public/*`.
 */
export function getPublicToken(): string {
  return "";
}
